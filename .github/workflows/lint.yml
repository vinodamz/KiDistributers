name: PHP lint

on:
  push:
    branches-ignore: [main]
  pull_request:

jobs:
  lint:
    name: php -l on every .php file
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          tools: none
      - name: php -l
        run: |
          set -e
          find . -path ./.git -prune -o -name '*.php' -print | while read -r f; do
            php -l "$f" >/dev/null
          done
